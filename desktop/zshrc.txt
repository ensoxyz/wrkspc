alias i.tar='echo "x --extract = extract files from an archive \n -v, --verbose = verbosely list files processed \n -z, --gzip = gzipped files eg. for tar.gz packages \n -f, --file ARCHIVE = use archive file or device ARCHIVE"'
alias c.zsh="nvim ~/.zshrc"
alias l.bin="cd /usr/local/bin && ls"

alias path='echo $PATH | tr ":" "\n" | nl'

alias i.wget='echo "wget -P /tmp -O /tmp/file.ext --https-only --no-cookies --no-cache --secure-protocol=PFS"'

alias i.git='echo "git add . |>  git commit -a -m |>  git push"'

alias c.kitty="nvim ~/.config/kitty/kitty.conf"

